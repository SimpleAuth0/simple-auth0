jar {
    archiveBaseName = 'simpleauth0-audit'
    archiveVersion = version
    archiveExtension = 'jar'
}

dependencies {
    implementation libraries.springBootStarterAop
    implementation project(':simpleauth0-core')
}
