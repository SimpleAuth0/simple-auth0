jar {
    archiveBaseName = 'simpleauth0-oauth2'
    archiveVersion = version
    archiveExtension = 'jar'
}

dependencies {
    implementation project(':simpleauth0-core')
    implementation libraries.springAuthorizationServer
    implementation libraries.springBootStarterOauth2Client
}
