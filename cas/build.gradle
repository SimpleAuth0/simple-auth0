jar {
    archiveBaseName = 'simpleauth0-cas'
    archiveVersion = version
    archiveExtension = 'jar'
}

dependencies {
    implementation libraries.springSecurityCas
    implementation project(':simpleauth0-core')
}
